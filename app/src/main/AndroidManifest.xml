<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developeralamin.ecommerceapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logop"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceApp">
        <activity android:name=".activities.PaymentActivity"/>
        <activity android:name=".activities.AddAddressActivity" />
        <activity android:name=".activities.AddressActivity" />
        <activity android:name=".activities.CartActivity" />
        <activity android:name=".activities.ShowAllActivity" />
        <activity android:name=".activities.DetailesActivity" />
        <activity android:name=".activities.OnBoardingActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_6eDVKGqZxSZUQF"/>

    </application>

</manifest>